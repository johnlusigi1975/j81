# Python
__pycache__/
*.pyc
*.pyo

# Virtualenvs (machine-specific, huge — never commit; Docker builds its own)
.venv/
**/.venv/
venv/

# Secrets — NEVER commit (set these in the host's env / Render dashboard)
.env
**/.env
*.env
connections.json
**/connections.json

# Runtime data (lives on the persistent disk in production)
data/
**/data/
out/
**/out/
logs/
**/logs/
*.db
*.db-wal
*.db-shm
**/config/research_config.json
